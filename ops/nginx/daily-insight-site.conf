server {
    listen 8080;
    server_name _;

    root /srv/daily-engineering-security-insight/site;
    index index.html;
    autoindex off;

    location / {
        try_files $uri $uri/ $uri/index.html =404;
    }
}
